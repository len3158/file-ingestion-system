version: '3.9'

services:
  java-api:
    build: ./java-api
    ports:
      - "8081:8080"
    volumes:
      - ./python-app:/app/python-app  # Share metadata

  python-ingest:
    build: ./python-app
    command: ./ingest.sh  # Or cron for periodic
    volumes:
      - ./python-app:/app/python-app