package com.example.fileingestionsystem;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration tests for the File Ingestion System application.
 * Verifies that the Spring context loads correctly without configuration errors.
 */
@SpringBootTest
class FileIngestionSystemApplicationTests {

    @Test
    void contextLoads() {
        // No assertions needed; test passes if context loads without exceptions
    }
}